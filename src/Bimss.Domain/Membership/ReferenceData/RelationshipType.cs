namespace Bimss.Domain.Membership.ReferenceData;

public sealed class RelationshipType : ReferenceDataItem
{
    public RelationshipType(Guid id, string code, string name)
        : base(id, code, name)
    {
    }
}
