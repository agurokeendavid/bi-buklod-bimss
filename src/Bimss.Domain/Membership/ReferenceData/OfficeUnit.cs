namespace Bimss.Domain.Membership.ReferenceData;

public sealed class OfficeUnit : ReferenceDataItem
{
    public OfficeUnit(Guid id, string code, string name)
        : base(id, code, name)
    {
    }
}
